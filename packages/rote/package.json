{
  "name": "@flow-build/rote",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "SKILL.md"],
  "scripts": {
    "gen": "node scripts/gen-skill.mjs",
    "prebuild": "node scripts/gen-skill.mjs",
    "pretypecheck": "node scripts/gen-skill.mjs",
    "pretest": "node scripts/gen-skill.mjs",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@flow-build/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  }
}
